# Install latest docker client
machine:
  environment:
   BIN_URL: "https://get.docker.com/builds/Linux/x86_64/docker-latest"
   BIN_PATH: "/usr/local/bin/docker"

dependencies:
  override:
    - sudo wget $BIN_URL -O $BIN_PATH
    - sudo chmod +x $BIN_PATH
    - rake build