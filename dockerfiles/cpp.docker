FROM gcc:latest

ENV FILE prog.cpp
ENV COMPILE g++ -o prog $FILE
ENV EXEC ./prog

ENV DEV /home/dev

COPY shared/* $DEV/

RUN useradd dev
RUN chown -R dev: $DEV

USER dev
WORKDIR $DEV

ENTRYPOINT ["./run.sh"]